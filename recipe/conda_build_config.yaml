# Required for UniformTypeIdentifiers
MACOSX_SDK_VERSION:        # [osx and x86_64]
  - "11.0"                 # [osx and x86_64]

# Workaround for https://github.com/conda-forge/manif-feedstock/issues/19, can be removed once pybind11 3.0.0 is released
numpy:
  - "1.26"
